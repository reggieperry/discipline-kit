__pycache__/
*.pyc
.DS_Store
*.swp

# Claude Code agent worktrees (isolation: worktree). Not excluded by the harness itself —
# measured 2026-09-10: .git/info/exclude is empty and the path reads dirty without this line.
.claude/worktrees/
