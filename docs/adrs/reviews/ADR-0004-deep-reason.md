# ADR-0004 acceptance gate: the deep-reason record

Passes per `harness/skills/adr-write/SKILL.md`.

## Pass 1 (pre-draft), 2026-08-12: the hypothesis dismantled, the grounds rebuilt

Attacked the proposal to decide substrate (iii)—wrapper sessions—on disinterest, bounded
context, and the probe's enabling facts. The verdict: the evidence permits (iii) and forces
nothing, and the grounds do not survive the raws. Findings taken:

- Five framing errors: the enabling facts enable every candidate; disinterest does not
  separate (ii) from (iii)—and leg 1 measures the wrapper *adding* a judgment layer that
  improvised a brief from the judged tree; the context criterion cuts against (iii), which
  keeps the subagent-overflow hole open while (ii) dissolves it; billing precedence was
  already VERIFIED and discriminates nothing; and the fork was under-enumerated—a fourth
  candidate (one long headless session driving all phases) had to be named and disposed.
- The audit of the probe record it triggered produced the same-day correction section: the
  one-source finding was confounded and a third leg discriminated it; the redaction
  disclosure was wrong twice, with slug survivors the scrub gate was blind to; §4.8's
  worktree-exclusion VERIFIED mark is measured false at 2.1.224.
- Missing decisions supplied: worktree provenance and placement; the wrapper-fails-closed
  and no-agents-directory rules; resume semantics; settings-source pinning as a Decision
  with a story-level court.
- Process obligations: take id 0004 and rewrite the five informal envelope references in the
  same commit; budget a new story (the coverage gate forces it into the landing commit);
  ADR-0003's Alternatives paragraph rejecting bare `-p` predates the measurement and had to
  be adjudicated rather than leaned on; retire the walkthrough's substrate assertion
  precisely, retaining its true half.

The draft then chose (ii)—bare headless invocation per phase, sequencer-owned worktrees
outside the parent—on the gate's own analysis: no intermediary judgment layer, documented
context behavior, the strongest resume story, worktree custody with the grader, and the
fence re-homed to per-invocation pinned settings hooks, which the probe measured firing
under bypass permissions.

## Pass 2 (draft attack)

Recorded after the draft attack runs.
